package hard;

import java.util.List;

/**
 * 给定一个单词和长度为L的数组，格式化文本，使每一行具有完全L个字符并且完全（左和右）对齐。
 * 你应该用一种贪婪的方式来包装你的话; 也就是说，可以在每行中包含尽可能多的单词。' '必要时填充额外的空格，以便每行都有L个字符。
 * 词语之间的额外空格应尽可能均匀分配。如果一行上的空格数量不能均匀地分配在单词之间，则左侧的空槽将被分配更多的空间，而不是右侧的空槽。
 * 对于文本的最后一行，它应该是左对齐的，并且在单词之间不会插入额外的空格。
 *
 * @author hdd
 */
public class TextJustification {
    public List<String> fullJustify(String[] words, int maxWidth) {
       return null;
    }
}
